{
  "name": "core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "module": "src/index.src",
  "types": "src/index.d.ts",
  "sideEffects": false,
  "exports": {
    ".": {
      "import": "./src/index.js"
    }
  },
  "scripts": {
    "clean": "rm -rf dist .turbo",
    "prebuild": "pnpm turbo run clean && tsc --build --clean",
    "build": "tsc --build && tsup"
  }
}
