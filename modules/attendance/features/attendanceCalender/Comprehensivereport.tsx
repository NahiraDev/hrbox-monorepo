import { AppInput } from '@core/components';

const Comprehensivereport = () => {
  return (
    <>
      <div className="flex w-[10%] flex-col h-full bg-primary-50 rounded-xl border-1 border-primary p-3 ">
        <h1 className="text-base font-semibold ">Comprehensive report</h1>
        <div className="w-full h-[1px] bg-[#E5E5E5] mb-[14xp]" />
        <div className="flex flex-col justify-center gap-4 mt-[14px] overflow-y-scroll ">
          <AppInput
            props={{
              label: 'Total Daily Working Hours',
              size: 'sm',
            }}
          />{' '}
          <AppInput
            props={{
              label: 'Total Daily Working Hours',
              size: 'sm',
            }}
          />{' '}
          <AppInput
            props={{
              label: 'Total Daily Working Hours',
              size: 'sm',
            }}
          />{' '}
          <AppInput
            props={{
              label: 'Total Daily Working Hours',
              size: 'sm',
            }}
          />{' '}
          <AppInput
            props={{
              label: 'Total Daily Working Hours',
              size: 'sm',
            }}
          />{' '}
          <AppInput
            props={{
              label: 'Total Daily Working Hours',
              size: 'sm',
            }}
          />{' '}
          <AppInput
            props={{
              label: 'Total Daily Working Hours',
              size: 'sm',
            }}
          />{' '}
          <AppInput
            props={{
              label: 'Total Daily Working Hours',
              size: 'sm',
            }}
          />{' '}
          <AppInput
            props={{
              label: 'Total Daily Working Hours',
              size: 'sm',
            }}
          />{' '}
          <AppInput
            props={{
              label: 'Total Daily Working Hours',
              size: 'sm',
            }}
          />{' '}
          <AppInput
            props={{
              label: 'Total Daily Working Hours',
              size: 'sm',
            }}
          />
        </div>
      </div>
    </>
  );
};

export default Comprehensivereport;
